[package]
name = "vector"
edition = "2021"
version = "0.0.0"

[dependencies]
hnsw_rs = "0.3"
serde = { version = "1", features = ["derive"] }
bincode = "1.3"
redb = "2"

[dev-dependencies]
tempfile = "3"
